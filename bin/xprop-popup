#!/bin/sh

PROPERTIES="$(xprop |
              egrep 'WM_(CLASS|NAME|WINDOW_ROLE)' |
              sed -e 's/&/&amp;/g')"
TITLE="X Window Properties"
TEXT="Identifing properties of the selected window:\n\n<tt>${PROPERTIES}</tt>"

zenity --info --title="${TITLE}" --text="${TEXT}"
