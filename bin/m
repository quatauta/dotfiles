#!/bin/sh

export EDITOR="vim -f"

for MUTT in neomutt mutt ; do
    if [ -x "$(which "${MUTT}")" ] ; then
        export MUTT
        break
    fi
done

"${MUTT}" -Z "$@" || "${MUTT}" -f = "$@"
touch /var/mail/daniel
