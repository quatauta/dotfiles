#!/bin/sh

source "${HOME}/.zsh/functions/prompt_git_status"

locate -r '/\.git$' |
sed -e 's|/\.git$||' |
while read DIR ; do
    pushd "${DIR}" >/dev/null
    GIT_STATUS="$(prompt_git_status '[' ']')"
    printf '%-20s %s\n' "${GIT_STATUS}" "${DIR}"
    popd >/dev/null
done
