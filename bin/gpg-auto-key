#!/bin/sh

GPG=`which gpg`

if [ -x "$GPG" ] ; then
    $GPG \
        --primary-keyring auto-pubring.gpg \
        --auto-key-locate "cert pka ldap keyserver" \
        --keyserver-options auto-key-retrieve \
        "$@"
else
    echo "Can't find gpg executable."
fi
