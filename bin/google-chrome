#!/bin/sh

CHROME_OPTS="--enable-dom-distiller"

for CHROME in /usr/bin/google-chrome{,-unstable,-beta,-stable} ; do
    if [ -x "${CHROME}" ] ; then
        break
    fi
done

exec "${CHROME}" ${CHROME_OPTS} "${@}"
