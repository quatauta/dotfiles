# Daniel Schömer's muttrc
# vim:syntax=muttrc:

# macros

macro index,pager \cb "|urlview\n" 'Display URLs with urlview'
macro index,pager + "<change-folder>+<enter>"
macro index O "<shell-escape>offlineimap<enter>"           "run offlineimap to sync all mail"
