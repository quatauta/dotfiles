# ~/.zsh/propmt
# vim:syntax=zsh:

autoload -Uz promptinit
promptinit

case $TERM in
    xterm*)
        precmd () {print -Pn "\e]0;%n@%m: %~\a"}
        ;;
esac

source "${HOME}/Dokumente/workspace/liquidprompt/liquidprompt"
true
